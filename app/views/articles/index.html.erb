<p style="color: green"><%= notice %></p>

<h1>Articles</h1>

<div id="articles">
  <div class="flickr_pagination">
    <%= will_paginate @articles if @articles.count > 0 %>
  </div>
  <% @articles.each do |article| %>
    <%= render article %>
     <hr />
  <% end %>
  <div class="flickr_pagination">
    <%= will_paginate @articles if @articles.count > 0 %>
  </div>
</div>
