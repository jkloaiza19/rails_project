<div id="<%= dom_id article %>">
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= article.description %>
  </p>
  
  <%= button_to 'Delete', article, method: :delete %><br />
  <%= link_to 'Update', edit_article_path(article) %>
</div>
